import { WalletTrackerContent } from "@/components/tracker/WalletTrackerContent";

export const metadata = { title: "Wallet Tracker — CryptoNinja" };

export default function WalletTrackerPage() {
  return <WalletTrackerContent />;
}
