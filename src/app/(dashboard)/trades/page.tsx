import { TradesContent } from "@/components/trades/TradesContent";

export const metadata = { title: "Trade History — CryptoNinja" };

export default function TradesPage() {
  return <TradesContent />;
}
