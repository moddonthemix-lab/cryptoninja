import { StrategyListContent } from "@/components/strategy/StrategyListContent";

export const metadata = { title: "Strategies — CryptoNinja" };

export default function StrategyPage() {
  return <StrategyListContent />;
}
