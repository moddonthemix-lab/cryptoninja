import { StrategyBuilderContent } from "@/components/strategy/StrategyBuilderContent";

export const metadata = { title: "Strategy Builder — CryptoNinja" };

export default function StrategyBuilderPage() {
  return <StrategyBuilderContent />;
}
